"""intelligence/admin.py — no models to register, but kept for completeness"""
from django.contrib import admin
# Intelligence engine has no DB models of its own.
# All data lives in teams, dependencies, notifications (AuditLog), etc.
